{
  "name": "@inventage/portal-components",
  "version": "0.0.0",
  "description": "A collection of webcomponents for enterprise portals.",
  "author": "Inventage AG <info@inventage.com>",
  "license": "MIT",
  "main": "index.js",
  "module": "index.js",
  "keywords": [
    "web-components",
    "lit-element",
    "lit-html",
    "portal"
  ],
  "scripts": {
    "start": "concurrently -n wca,storybook 'npm run watch:custom-elements' 'npm run storybook'",
    "start:build": "npm run build && npm run storybook:build:start",
    "build": "run-s build:**",
    "build:storybook": "npm run storybook:build",
    "build:wca": "wca analyze components/*/*.js --outFile custom-elements.json",
    "format:eslint": "eslint --ext .js,.html . --fix --ignore-path .gitignore",
    "format:prettier": "prettier '**/*.{js,md}' --write --ignore-path .gitignore",
    "lint": "run-p lint:*",
    "lint:eslint": "eslint --ext .js,.html . --ignore-path .gitignore",
    "lint:prettier": "prettier '**/*.{js,md}' --check --ignore-path .gitignore",
    "lint:analyze": "lit-analyzer components --strict",
    "format": "npm run format:eslint && npm run format:prettier",
    "test": "karma start --coverage",
    "test:watch": "karma start --auto-watch=true --single-run=false",
    "storybook": "start-storybook",
    "storybook:build": "build-storybook",
    "storybook:build:start": "es-dev-server --root-dir storybook-static --open",
    "watch:custom-elements": "onchange -i 'components/**/*.js' -- npm run build:wca"
  },
  "dependencies": {
    "lit-html": "^1.2.1",
    "lit-element": "^2.3.1"
  },
  "devDependencies": {
    "@lion/helpers": "^0.4.0",
    "@open-wc/demoing-storybook": "^2.2.12",
    "@open-wc/eslint-config": "^2.0.6",
    "@open-wc/testing": "^2.5.16",
    "@open-wc/testing-karma": "^3.3.28",
    "@storybook/addon-a11y": "^5.3.18",
    "@storybook/addon-actions": "^5.3.18",
    "concurrently": "^5.2.0",
    "deepmerge": "^4.2.2",
    "es-dev-server": "^1.50.6",
    "eslint": "^6.1.0",
    "eslint-config-prettier": "^6.11.0",
    "husky": "^4.2.5",
    "lint-staged": "^10.2.4",
    "lit-analyzer": "^1.1.10",
    "npm-run-all": "^4.1.5",
    "onchange": "^7.0.2",
    "prettier": "^2.0.5",
    "web-component-analyzer": "^1.0.3"
  },
  "eslintConfig": {
    "extends": [
      "@open-wc/eslint-config",
      "eslint-config-prettier"
    ]
  },
  "prettier": {
    "singleQuote": true,
    "arrowParens": "avoid",
    "printWidth": 120,
    "trailingComma": "all"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
