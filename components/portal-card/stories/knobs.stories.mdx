import { withKnobs, withWebComponentsKnobs, html, Story, Meta, text, Preview } from '@open-wc/demoing-storybook';

import '../portal-card.js';

<!-- @see https://github.com/open-wc/open-wc/edit/master/packages/demoing-storybook/demo/stories/withWebComponentsKnobs.stories.mdx -->

# Card

<Meta
  title="Components/Card/Knobs"
  decorators={[withKnobs, withWebComponentsKnobs]}
  parameters={{ component: 'portal-card', options: { selectedPanel: 'storybookjs/knobs/panel' } }}
/>

## Example output

Don't forget to switch to Canvas if you wanna see the knobs.

<Story name="Knobs">{html` <portal-card title="Harry Potter"></portal-card> `}</Story>

## Provide own light dom

By adding knobs within the template you can provide additional knobs for this content.

<Preview withToolbar height="500px">
  <Story name="Provide own light dom">
    {html`
      <portal-card>
        <p>${text('Content', 'Some text', 'Properties')}</p>
      </portal-card>
    `}
  </Story>
</Preview>

## Multiple components

Even if you have multiple components in your dom you will have knobs for all of them.

<Story name="Knobs for current Component">
  {html`
    <portal-card title="Harry Potter">The main character</portal-card>
    <portal-card title="Lord Voldemort">An evil wizard</portal-card>
  `}
</Story>

That means however that it will only shown knobs for the same elements.

## Provide own querySelectorAll

If you have different elements or want to show knobs for a specific element you can provide your own querySelectorAll.

The parameter is `parameters.customElements.querySelectorAll`.

<Story
  name="Provide own querySelectorAll"
  parameters={{ customElements: { querySelectorAll: 'portal-card:nth-of-type(1)' } }}
>
  {html`
    <portal-card title="Harry Potter">The main character</portal-card>
    <portal-card title="Lord Voldemort">An evil wizard</portal-card>
  `}
</Story>

This is a full querySelectorAll string so here are some examples of what it will look like

- `portal-card:nth-of-type(1)` will only show knobs for the first element
- `portal-card:nth-of-type(2)` will only show knobs for the second element
- `*` will add knobs for all known\* custom elements
- `.content *` will add knobs to all known\* custom elements within the `.content` element
- `div` will throw as no valid custom element can be selected

\* listed in `custom-elements.json`
