version: 2
updates:
- package-ecosystem: npm
  directory: "/"
  schedule:
    interval: daily
    time:
  allow:
    - dependency-type: "direct"
    - dependency-type: "production"
  open-pull-requests-limit: 10
